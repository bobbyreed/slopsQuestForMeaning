{
  "name": "slops-quest-for-meaning",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && firebase deploy",
    "test": "vitest",
    "test:run": "vitest run",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "phaser": "^3.88.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.4",
    "vite": "^6.3.2",
    "vitest": "^4.1.4"
  }
}
